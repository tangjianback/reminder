server:
  port: 8080
spring:
  servlet:
    multipart:
      max-file-size: 1000MB
      max-request-size: 1000MB
    thymeleaf:
      prefix: classpath:/templates
      suffix: .html
      cache: false
